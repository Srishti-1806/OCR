fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
pydantic==2.7.4
surya-ocr
# OCR — same PP-OCR models as PaddleOCR, but runs via ONNX Runtime instead of
# the native paddle framework. Avoids paddlepaddle's oneDNN/fused_conv2d
# incompatibility entirely (no paddle install needed at all).
rapidocr-onnxruntime==1.3.24

# PDF -> Image
pdf2image==1.17.0

# Fallback: native text-layer extraction for born-digital PDFs (no OCR needed)
PyMuPDF>=1.26.7

# Image processing
opencv-python-headless==4.10.0.84
numpy==1.26.4
Pillow==10.3.0

# Fuzzy label matching (optional, used only if you add rule-based fallbacks)
rapidfuzz==3.9.4

# LLM (Extraction Service) — internal MCP LLM client via HTTP
httpx==0.27.0